/*
export default function MatchSummary() {

    const {
        teamAName,
        teamBName,
        teamAScore,
        teamBScore,
        halftimeScore,
        date,
    } = match;

    return (
        <div className="bg-white shadow-md rounded-lg p-4 mb-4 text-center">
            <div className="text-sm text-gray-500">{new Date(date).toLocaleDateString()}</div>
            <div className="flex items-center justify-center text-2xl font-bold my-2">
                <span className="text-gray-800">{teamAName}</span>
                <span className="mx-4 text-blue-600">
          {teamAScore}:{teamBScore}
        </span>
                <span className="text-gray-800">{teamBName}</span>
            </div>
            <div className="text-sm text-gray-600">(HT: {halftimeScore})</div>
        </div>
    );
}
*/
